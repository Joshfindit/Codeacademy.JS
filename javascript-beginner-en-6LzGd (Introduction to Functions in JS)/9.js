// Write your function starting on line 3

var perimeterBox = function (length, width) {
    var perimeter = length + length + width + width;
    return perimeter;
};

console.log(perimeterBox(6, 9));
